// Include the preprocessing libraries needed for the code to work
#include <iostream> // handles basic inpout/output
#include <string> // handles text data like a boss
#include <array> // handles array operations
#include <cstdlib> // allows for use of basic functions

using namespace std; // used to prevent naming conflicts when using multiple libraries (encapsulates identifiers for classes, functions, variables, etc..) 

// Predeclared functions
string grimDarkSetting(string name);
void theChapel();
void priceForSpeaking();
void listeningPaths();
void seeTruth();
void bossBattle();

int main() {
	// GREET THE PLAYER
	string playerName = " "; // Declare variable to gather player input, ensure capture of everything the player types
	cout << "\n   ==== Welcome to the town of WhisperingToll ==== \n\n"
		<< "        Who enters these lands:\n\n";
	getline(cin, playerName); // Capture the entire line the player types into the game
	cout << "\nGo Back, " << playerName << "... NOW!\n\n";

	// CALL THE FUNCTIONS
	cout << grimDarkSetting(playerName); // Display the return statement for the fucntion

	
}

/* ==== ADD ALL FUNCTIONS BELOW MAIN ==== */

// SCENARIO 1: SET THE TONE, SETTING, AND THE FIRST CHOICE THAT LEADS TO SCENARIO 2 OR SCENARIO 3
string grimDarkSetting(string name) {
	string playerName = name;
	// Paragraph One sets the dark tone
	cout << "\nThe night is a wound across the sky stitched with cold stars and the promise of something watching.\n"
		<< "You wake in a gutter of black mud beside a road of ancient stones, slick as old bones. Ahead, the ruins\n"
		<<"of Kareth Vane rise like broken teeth. Behind you, the forest presses close: the Wyrmwood, where trees\n"
		<< "grow in spirals as if listening to secrets.\n\n";

	// Paragraph Two engages the player and offers the first choice
	cout << "A bell tolls from the ruins; slow, wrong, and far too close for its distance. Each note vibrates in your\n"
		<< "ribs like a remembered sin. Your hands are empty, but your name feels heavy, as if it once belonged to someone braver.\n\n"
		<< "A raven lands on a milestone and speaks with a human tongue:\n";
		

	// Player will choose one of the two options with input validation
	char choice;
	while (true) {
		cout << "\"Choose quickly. The dead are impatient.\"\n\n"
			<< "Type A to approach the ruins of Kareth V, or\n"
			<< "Type B to Enter the Wyrmwood and follow the whispering trees. ";
		cin >> choice;

		// Add input validation
		choice = toupper(choice); // Ensure the letter entered is upper case so it passes the thru control flow smartly 

		if (choice == 'A') { theChapel(); break; } // Head to Scenario 2, break exits the loop
		else if (choice == 'B') { listeningPaths(); break; } // Head to Scenario 3, break exits the loop
		else { cout << "\nYou must choose either A or B\n\n"; } // Provide input validation message
	}
	return "Let's move on, " + name + ".\n\n";
}

// SCENARIO 2: LEADS TO SCENARIO 2A OR BOSS BATTLE
void theChapel() {
	// Continue the narrative generated by OpenAI...Claude would have been a better choice
	cout << "\n\nKareth Vane is quieter than it should be. Not peaceful—just… held. The air smells of soot, dried blood, and rain that never arrived.\n"
		<< "You push through a shattered archway into a chapel whose roof has collapsed, leaving the altar open to the sky.\n"
		<< "Candles burn anyway, hundreds of them standing in melted ranks across the stone. Their flames do not flicker. They do not bow to wind.\n"
		<< "They burn like vows made under duress.\n\n";

	cout << "At the altar, a stone saint has been carved without eyes. Someone has placed fresh teeth in the empty sockets, small and human.\n"
		<< "A figure kneels with its back to you, draped in mourning cloth. When it turns, you see a face you recognize from childhood nightmares:\n"
		<< "the Pale Confessor, mouth stitched shut with silver wire. It raises a hand and points to two offerings laid before the altar:\n\n"
		<< "   * A tarnished iron reliquary that hums faintly, as if something inside wants out.\n"
		<< "   * A black ink-stained oathscroll, already bearing your signature in a handwriting you don’t remember making.\n\n"
		<< "The Confessors fingers twitch. The candles lean toward you like eager witnesses.\n";

	// Player will choose one of the two options with input validation
	char choice;
	while (true) {
		cout << "\"Choose quickly. The dead are impatient.\"\n\n"
			<< "Type A to take the oathscroll and read it aloud, or\n"
			<< "Type B to take the iron reliquary and break its seal ";
		cin >> choice;

		// Add input validation
		choice = toupper(choice); // Ensure the letter entered is upper case so it passes the thru control flow smartly 

		if (choice == 'A') { priceForSpeaking(); break; } // Head to Scenario 2, break exits the loop
		else if (choice == 'B') { bossBattle(); break; } // Head to Scenario 3, break exits the loop
		else { cout << "\nYou must choose either A or B\n\n"; } // Provide input validation message
	}
}

// SCENARIO 2A: PLAYER DIES IN HERE AND IS ASKED TO PLAY AGAIN
void priceForSpeaking() {
	cout << "Your voice should be your own. It isn’t.\n"
		<< "The first words you read are not words at all—they are hooks.They sink into your throat, your lungs, your marrow.\n"
		<< "The chapel exhales, satisfied.The candles flare blue, and the stitched mouth of the Confessor smiles without moving.\n"
		<< "Your tongue blackens into ash.Your eyes fill with candlewax.You try to scream, but the oath is already doing the screaming for you.\n"
		<< "Above, the open sky bends inward like a lid closing on a coffin.\n"
		<< "The last thing you feel is the gentle pressure of a hand on your shoulder—comforting, almost kind—before the chapel swallows your name whole.\n\n";

	// Control the flow of the choice
	char choice;
	cout << "You have died. Play again(Y/N)? ";
	cin >> choice;
	choice = toupper(choice); // ensure the char will be cast to uppercase to make the code easier to read

	if (choice == 'Y') { theChapel(); } // send player back to chapel
	else if (choice == 'N') { cout << "\n\nGoodbye.\n\n"; } // end the game
	else { cout << "\nYou must choose either A or B\n\n"; }	// data validation
}

// SCENARIO 3: LEADS TO SCENARIO BOSS BATTLE OR 3B
void listeningPaths() {
	// Paragraph One sets the dark tone
	cout << "The forest accepts you the way deep water accepts a stone: without hesitation, without mercy.\n"
		<< "The trees are tall and twisted, bark folded like scar tissue. You notice carvings in the trunks...not runes,\n"
		<< "not letters, just tally marks in uneven rows, as if the forest is counting something it cannot afford to forget.\n"
		<< "Your footsteps do not echo. Instead, you hear other footsteps matching yours, always one pace behind.\n\n"

		<< "Fog coils between the roots. In it, shapes drift, almost human, almost familiar wearing faces that do not fit them properly.\n"
		<< "A childs laughter slips through the branches, bright and wrong.\n\n"

		<< "You come upon a pool as black as ink. Suspended above it, hanging from thorn-vines, is a cage of bone.\n"
		<< "Inside the cage: a lantern still lit, its flame trembling like a fearful heart. Beneath the cage, at the pools edge,\n"
		<< "lies a sword half buried in moss, its blade pitted but real.\n\n"

		<< "The air tasts like iron and death. A voice whispers behind you...\n";
		

	// Player will choose one of the two options with input validation
	char choice;
	while (true) {
		cout << "\"Only one choice will let you escape me.\"\"\n\n"
			<< "Type A to take the sword and cut down the bone cage lantern, or\n"
			<< "Type B to kneel and drink from the black pool and see the truth. ";
		cin >> choice;

		// Add input validation
		choice = toupper(choice); // Ensure the letter entered is upper case so it passes the thru control flow smartly 

		if (choice == 'A') { bossBattle(); break; } // Head to Scenario 2, break exits the loop
		else if (choice == 'B') { theChapel(); break; } // Head to Scenario 3, break exits the loop
		else { cout << "\nYou must choose either A or B\n\n"; } // Provide input validation message
	}
}


// SCENARIO 3B: PLAYER DIES IN HERE AND IS ASKED TO PLAY AGAIN
void seeTruth() {

	// Control the flow of the choice
	char choice;
	cout << "You have died. Play again(Y/N)? ";
	cin >> choice;
	choice = toupper(choice); // ensure the char will be cast to uppercase to make the code easier to read

	if (choice == 'Y') { theChapel(); } // send player back to chapel
	else if (choice == 'N') { cout << "\n\nGoodbye.\n\n"; } // end the game
	else { cout << "\nYou must choose either A or B\n\n"; }	// data validation
}

// FINAL SCENARIO: BOSS BATTLE; PLAYER FIGHTS AND DIES OR FLEES AND SURVIVES - BOSS TO POWERFULL
void bossBattle() {
	cout << "The world narrows into a single place, as if reality itself is bracing for impact.\n"
		<< "You stand in a circular ruin—an arena of broken pillars and wet stone—beneath a sky that churns like a bruise.\n"
		<< "In the center squats a throne grown from twisted roots and fused bone. Upon it sits the Thorn-Crowned Hunger,\n"
		<< "the thing that has been wearing the night like a cloak.\n\n"
		
		<< "It rises. Not quickly—never hurried—because it has nowhere to be except inside your ending.\n\n"
		
		<< "Its body is a cathedral of rot and regalia: ribs plated with black iron, a crown of thorns hammered through living flesh,\n"
		<< "and a face that shifts between beautiful and unbearable. When it speaks, the sound is not heard so much as remembered.\n\n"
		
		<< "\"You came,\"" << " it said, assuming you signed the contract. " << "\You ALWAYS come\"\n\n"
		
		<< "The fight begins the moment you understand you are already losing. Your strikes feel like arguments made to a judge\n"
		<< "who stopped caring centuries ago. Your breath burns.Your arms tremble.Your vision tunnels.Still, you remain standing.\n\n"
		
		<< "The Hunger leans close. You smell grave-dirt and sweet incense."
		<< "\Rally little ember....or runnnnnnn!\" ";


	// Control the flow of the choice
	char choice;
	cout << "You have died. Play again(Y/N)? ";
	cin >> choice;
	choice = toupper(choice); // ensure the char will be cast to uppercase to make the code easier to read

	if (choice == 'Y') { theChapel(); } // send player back to chapel
	else if (choice == 'N') { cout << "\n\nGoodbye.\n\n"; } // end the game
	else { cout << "\nYou must choose either A or B\n\n"; }	// data validation
}



/* === INTERACTIVE SIMULATION GUIDELINES ===

	Create an Interactive Simulation Application.
	Create a C++ program that uses more than one function.
	Control all your functions through the main function.
	Pass and return parameters to your functions.
	Your program must do something and be an interactive fiction game, it must not just demo functions.

   === PRO TIP - USE A LOT OF FUNCTIONS to write less code ===


   === EXPECTATIONS ===
	DONE. Create a new Empty C++ project.
	Create an Interactive Simulation Application with a great user experience.
	Your program must do something and be an interactive fiction game or simulation, it must not just demo functions.
	Add at least one function that takes a string or an int.
	Add at least one function that returns a string or an int.
	Add at least 5 functions overall in addition to the main function.
	Your main function should control all the other functions.
	Over comment your source code file (.CPP).
	Take a screenshot of the program working.
*/
